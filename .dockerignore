# Keep all workspace members (crates/, tools/) + Cargo.toml/Cargo.lock so
# `cargo build -p plsql-mcp` resolves the workspace. Exclude build output, VCS,
# fixtures, and docs/state dirs that the build never reads.
target/
.git/
.github/
.beads/
.claude/
.ntm/
.usr/
corpus/
docs/
*.md
