# Historical Architecture Note

This lowercase file is retained only so old links do not break.

Read [`ARCHITECTURE.md`](ARCHITECTURE.md) for the current architecture.
The current project is an offline PL/SQL intelligence engine; MCP
transport and live Oracle database operation belong to `oraclemcp`.

The older May 2026 layer diagram that used to live here described a
pre-pivot server layout and is no longer maintained.
